Generating the Docs
===================

* the following packages are required to build the documentation:
   - sphinx
   - sphinx_rtd_theme

*  generate the docs:
    - from sauceclient main directory, run:
      $ sphinx-build -b html doc ./docs

   html documentation gets generated in the `sauceclient/docs` directory.
